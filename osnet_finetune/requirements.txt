# PyTorch는 CUDA 버전에 맞춰 별도 설치 (README 참조)
# torch>=2.2,<2.4
# torchvision>=0.17,<0.19

# Detection / Tracking
ultralytics>=8.2.0          # YOLOv8 + 내장 ByteTrack
lap>=0.4.0                  # Hungarian (tracking 보조)
opencv-python>=4.9.0
numpy>=1.24,<2.0
pandas>=2.1.0
Pillow>=10.0.0
scikit-learn>=1.3.0
tqdm>=4.66.0
pyyaml>=6.0

# Re-ID
# torchreid는 README의 git clone + setup.py develop 권장
gdown>=5.1.0                # torchreid pretrained weight 다운로드용
scipy>=1.11.0

# UI
streamlit>=1.32.0

# Export
onnx>=1.16.0
onnxruntime-gpu>=1.17.0     # CPU only면 onnxruntime 로 교체
onnxsim>=0.4.36
